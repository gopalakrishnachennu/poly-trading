# Phase 1.9 Plan

1. Define the canonical durable command schema and conservative decoder.
2. Implement journal-first coordinator application and strict replay.
3. Add prefix checkpoint creation and validation.
4. Add bounded single-writer runtime, snapshots, restart, and shutdown.
5. Verify failure paths, replay equivalence, and formal durability invariants.
